[package]
name = "mawr-actions"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false
repository.workspace = true
rust-version.workspace = true

[dependencies]
mawr-core = { path = "../mawr-core" }
mawr-native-static = { path = "../mawr-native-static" }
mawr-semantic-html = { path = "../mawr-semantic-html" }
mawr-state = { path = "../mawr-state" }

[dev-dependencies]
mawr-observation = { path = "../mawr-observation" }
tokio = { version = "1.53.1", default-features = false, features = ["io-util", "macros", "net", "rt-multi-thread", "sync"] }

[lints]
workspace = true
